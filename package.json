{
  "name": "sandbox-game",
  "version": "1.0.0",
  "description": "IPenno - 2D Sandbox Game - Growtopia/Terraria inspired",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "serve": "python3 -m http.server 8000",
    "deploy": "npm run build && npx firebase-tools deploy --only hosting"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.80.0",
    "firebase": "^12.6.0"
  }
}
